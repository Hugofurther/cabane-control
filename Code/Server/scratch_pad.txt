{
    "token": "

eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpZCI6MSwidXNlcm5hbWUiOiJhZG1pbiIsInJvbGUiOiJBRE1JTiIsImlhdCI6MTc2NDQ1NzU0MCwiZXhwIjoxNzY0NTAwNzQwfQ.F5l0Uopjj0Sz2vL2wTxR47hUr_xSNvNwV0U5MIcRv_o

",
    "username": "admin",
    "role": "ADMIN"
}

-ethereum -metamask -wallet -ethers

First:
<server.js terminal 1>
[CONTROL] Taken by User: admin
[UDP] Sent Override: TAKE
</server.js terminal 1>

<main controller monitor 1
09:34:32.114 -> [REMOTE] Control TAKEN by Web App
</main controller monitor 1>


Then:
<server.js terminal 2>
[CONTROL] Released to CABANE
[UDP] Sent Override: RELEASE
[SYNC] Main is in Override. Pi assuming SERVER Control.
</server.js terminal 2>

<main controller monitor 2>
09:34:55.689 -> [REMOTE] Web App released (Pending Manual Reclaim)
</main controller monitor 2>

-------------------------------------------------------
-------------------------------------------------------


Done

Logging

√ Delete User

√ Users pending for more than 3hrs get deleted automatically.

√ Can we make the username case insensitive?

√ Can we have the option to see the password.

√ a user should be able to change their email and password

√ We need to setup the Pi Server to start the server.js automatically on boot.

√ boot automatically with watchdog

√ Admin can grant a user to be able to take control or not.

√ Changes in the switching should be logged on the server with the controlling user / device

√ Grant or deny user access to logs.

√ Add a clock

√ We should have the user of the logged in account displayed somewhere.

√ Give a way to search and sort the logs

√ Allow export logs to expert .csv

√ Search logs should be more like a filter for each column. If you click on filter button an input field appears above each column to insert a filter condition (e.g. > 12/3/25 (calendar or input field )| System ( dropbox menu ) | Type Dropbox

√ I feel like the username should be placed between the "Take Control" button and the "logout" button.

√ The Clock should be bigger.

√ Can we have the log window updating live?

√ In admin panel, add a way to set the time zone.

√ The user settings should be accessible by clicking on the username just left of the logout instead of the gear icon.

√ in the LogViewer, for the date, can we add the option to select a specific date / range of dates / later or earlier than a specific date.

√ The LogViewer isn't updating as fast as the switches on the interface. Is there a way to make it as responsive as the interface?

√ Since the user settings is now accessible via the username button, we don't need the gear icon anymore.

√ In the LogViewer, for the date, we are missing the "range" (from [date] to [date]

√ In the AdminPanel, the system settings' layout is not configured properly. Also, clicking the Save configuration should dismiss the panel. 

√ Is the Pi Server adjusting its clock to the internet time? -> Yes

√ You did not respond to the "I changed the time zone, but the clock did not update.". Can we have a more extensive list of timezones. It must include "Montréal, QC (EST)", "Drummondville, QC (EST)" and "Sainte-Brigitte-des-Saults, QC (EST)"

√ For the logviewer. I need a reset button to reset all filters. I also need the sort by timestamp Ascending or Descending button we used to have.

√ Also, we should have a user settings for the clock to display 24hrs or 12hrs.

√ Clicking outside a pop-up panel should dismiss the panel.

√ Can we fetch on the internet the temperature of a set location by the admin?

√ Can we add the option to display Celsius or Farenheight

√ Display the Pi Server disk usage in admin panel

√ With the clock, We can remove the seconds and the timezone please. We need to save space. Also, the 

√ Any search input field should respond to the key "enter"

√ With the Clock display, Can we make the AM/PM small and closer to the time.

√ The temperature "FEELS" and Location can be stacked on different lines.

√ We should stack the themperature above the Feels and Location stack and center them all together.

√ The reminder chirp is not working well anymore. Can you look into it? The first chirp should happen as soon as the buzzer switch is off and alarms stop. Let me know if you want to see my code.

√ In the Header, the Master should point to the name of the user instead of showing "USER". If no user has control, but the server still has control it should display "Master: Server". If the Main Controller has control, it should keeps displaying "MASTER: CABANE".

√ A user who has control granted can only control if it has control. As it is right now, which is not what is wanted, if any user or the server has control, all user can control.

√ Main Controller cannot take control back when pressing 2 buttons. It can Only takes control when a user releases to cabane from the app. It appears we have broken this logic when we were having issues with the "Take Control" button, meanwhile the issue was simply the route was corrupted. Here's the logic for the Main Controller taking control:
	- The Main Controller button index 0 and 1 pressed for 2 seconds.
	- Main Controller reboots or simply goes offline and becomes online again.

The Main Controller did not change anything in its sketch, so the issue is on the app side.

√ When the Main Controller becomes online again, it should regain control automatically disregard of who has control at the moment or who had control when the Main Controller lost control.

√ When a user has control and releases control to the Main Controller, If the Main Controller is offline, the control should automatically go to the Server. Right now, the control is released to the Main Controller even if it is not online.

√ There should be an option in the user settings to display the status of the Main Controller on the main page.


√ for the Weather component:
	- Does it update the temperature automatically?
	- Can we have one location or a rotation of multiple locations?

√ Can we have in the setting the enabling/disabling of stations, like the Main Controller has?

√ Can we make the weather component update every minute? Or have a setting to set the update frequency?

√ Let's add Wind Speed and Precipitation to the weather component.

√ Let's implement the messaging system
	- Messages should pop-up on the screen in a Message Panel.
	- User setting
		- dismiss messages after x minutes

√ Clicking outside of the Chat Panel should dismiss it.

√ We should assign different colors for each of the users to differenciate them.

√ Where could we put the list of users indicating which ones are online and the one offline.

√ The current user's messages should be displayed aligned to the right and others should be aligned the left as they are. 

√ This was a nice implementation of the "General Chat". Can we now add private chats and group chats?

√ The global Tab seems to be disconnected from the messages we already had in the messages table.

√ In Groups tab, Each group should display the name of its members. In the Group list. Once in the group, there should be a truncated list of the members below the group name, which fully expands if clicked on it.

√ When sending a new message, the message appears higher in the thread at first, then moves to the correct place. Can we have the new message being displayed at the proper place in the thread right away?

√ In the Group chat window, there should be a truncated list of the members below the name of the group. When clicking on the truncated list, it expands fully.

√ I notices when a user takes control, another user cannot control the switches which is great, but the StationCard background becomes light colored like if it had control. Can we keep it dark if the user doesn't have control? The only time the StationCard gets the light background is when the current user has control.

√ A user should have the option to delete a message they sent. Not other users messages.

√ The date should be displayed with the timestamp of the message unless the date is today. If it's yesterday, it should display yesterday, otherwise the date.

√ A user should have the option to remove themselves from a group. When the group is empty, the group gets deleted, but the messages remain.

√ I'm still getting stubborn badges that should not be present on the message icon on the main page. Could it be due to some elements that should be flushed?

√ Clicking on an "Urgent Message" dismisses it's urgency and the message return to a normal color.

√ Set weather to update once per update frequency to avoid 429 (too many requests)

√ When a user acknowledge a flash message in FlashViewer, the system behaves the same as if the user had seen it in the thread and dismisses it as being unseen.

√ For the messaging system. I want to add an optional feature, where when selected, the message sent will be displayed in a separate very obvious and impossible to miss pop-up window in the center of the screen taking about 60% -75% of the screen. This window will replace or use the "Urgent" feature, but replace the "Rappel" banner.
	- If a user sends a message with this option. The receiver will receive the message in the thread, but the Pop-up Window will appear displaying the message. When dismissed the message remains in the chat thread ( Global or private ).
	- This Pop-up window will also be used if a user clicks on a message. The message will open and be displayed in this pop-up.
	- Another way to use this pop-up is with the notes:
		- If a user creates a note with this option selected the note will appear in this Pop-up automatically.
		- When a user clicks on a note, The pop-up appears and the note is displayed in it.

	√ When such a pop-up window appears on a user interface, it remains there until dismissed, even if the user logs out. When logging in again the pop-up show up again until dismissed.

	√ When multiple pop-ups occur, they are stacked on top of each other and dismissed one at a time. The newer message on the top. There should be a way to scroll through multiple pop-ups without dismissing them.

	√ This window should be dismissible by either clicking on a dismiss button or click outside of the window.

	√ If the message is too long to display within the size of the pop-up window, the user can scroll. Swiping will only affect the pop-up if the mouse cursor is over the window. If the mouse cursor off of the window, the swiping scrolls the main window or the MessageDrawer, which ever the mouse cursor is over

	√ What could we call this optional feature?

√ I like the way all Flash Message" are added in the same window with arrow browsing. Can we make it that each of the "Flash Message" need to be "Acknowledged" separately and independently from each other. We can use the same button, but each message need to have its page's "acknowledge" button clicked.

√ in the FlashViewer of the recipient, can we have a list or who the Falsh Message was sent to (e.g. Global, Group 1, User 1)

√ A new message appears at the wrong place in the thread of a receiver at first. The receiver should also have a new message appear at the bottom of its thread automatically before the timestamp is calculated and the message is placed in the corned timestamp order.

√ when a new message gets added, is there a way to make if obvious to the user there are more message if you scroll down? Or should we simple get the thread window to scroll to the last message automatically when they come in?

√ The urgent message dismissal should only happen on the current user. Any other user which did not dismiss the "Urgent" should still see the message as "Urgent"

√ Let's add a badge to the envelope icon when there are new messages with the number of new messages. ( think iOS )

√ When new messages are present and the user clicks to open the MessageDrawer, a badge with the number of new messages are on the "Chat" tab if new messages in general chat. If new messages are in Users or Groups, a badge is display on these, the same.

√ Trying to fix the "thrashing" / race condition when new message is send and being displayed on the sender's thread.

√ Implementing the "Optimistic UI with Stable Keys" for sending messages.

√ Do not force New Messages" button to appear (setShowScrollButton(true)) when the length of the messages is smaller than the thread window.

√ Message Unseen Badges: When a message is unseen, the message icon on the main page displays the correct number of "unseen messages". In the header of the MessageDrawer, Each tab should display if the user have unseen messages in them. It used to work, but currently no badges on the MessageDrawer tab are working.

√ Flash Messages -> Logic.
√ When a user acknowledges a FlashMessage, the message should be downgraded in the thread as acknowledgement. As it is, the user needs to get out and back in the message thread for the downgrade to happen.

√ a sender of a flash message should be able to downgrade their message. When this happens, it should update the recipients by closing their FlashViewer and downgrading the message in there chat.

√ for messaging and notes, SHIFT + RETURN give a new line.

√ When in a group and clicking on "group" tab, the user should go back to the Group list.

√ When writing a message and selecting "Flash Message" this should not take the "focus" away from the text input box and pressing return should send the message the same as if we select the input box again after clicking on the "Flash Message" button.

√ the creator (admin) of a group should be able to rename the groups. When this happens, A FlashView memo should appear in all the members window to be acknowledged.

√ the creator (admin) of a group should be able add or remove members of a group quietly or with a FlashView Memo ( to the group ( publicly ) or just the member being added / removed ( Privately )

√ Group names should be constraint to unique per users (e.g.: if there is an existing group named "Maple" with users "A" and "B", user "C" cannot name a new group with user "A" and/or user "B", but user "C" can create a new group named "Maple" with user "D". If a user tried to create a group with a name constraint, the user gets prompt the name is already taken. 

√ Clicking on the Users tab gives me a blank screen

√ When created a new Group, the message auto generated 'Group "test7" created by hugo' should be sent at "Flash Message".

√ in the header of a group message, a list of the members should be listed under the name of the group in an expandable truncated label.

√ the admin of a group can delete a group and/or move its "admin status" to another member.

√ When adding a user and sending a public Flash Message, the name of the user being added or removed should be indicated.

√ Changes in groups should reflect live in each of the group in the groups table

√ When a user has been removed from a group, they should not get the badge notification when a new group message is sent.

√ the badges for new unseen message for a group should be displayed in red and not blue on the specific group in the Groups tab.

√ Only the administrator of a group can access the administration part ( the gear icon in of the group )

√ When a user get's removed from a group, since they are not receiving the group messages and need to be notified when "Public" is selected, in addition to send the notification to the Group, the removed user should be notified in a DM.

√ let's make the default modification the default notifyType in the group admin to a usersetting.

√ Storage usage displays "Unknows" can you tell me why?

√ When the Main Controller is offline, the Button "Release to Cabane" should become hidden. If for some reason the user succeeds releasing to cabane even if the Main Controller is offline, Control should automatically revert to "System_Failsafe" ( Server ), as if the Main Controller were to get disconnected from the network.

√ When the Main Controller has control and the vacuum switch is OFF, but then the Thermostat overrides it to ON, the vacuum switch started flickering between ON and OFF. Not sure, but I think it's because since the switch was OFF and the thermostat telling the switch to be on, there was an alternating change of what to listen to. Can you evaluate the code and make. Can you figure out the source of this problem please.

√ is there a way for me to ssh into the server fro a remote location?

√ When a station become offline, The UI should retain the last state of that station switches until it reconnects

√ make login expiresIn a user setting with default 60 days.

√ Can we make all native browser alerts a centered pop-up more of the same theme as the app. It is disorienting and bad UX to have those native browser alerts.

√ Can we make the wind direction of a more evident icon? this icon is a bit difficult to discern.

√ in the Weather pop-up modal, the snowflakes icon is very dark and we can barely see it . Is the

√ for the weather pop-up modal, the 'pause feature' resumes to rotating after the rotating frequency time has passed. It's like it's not paused. Can we make it so it really pauses and then resume when clicking the pop-up modal again, outside the arrows.

√ we should add a "save settings" button for the user settings panel like we have for the Admin Panel

√ In AdminPanel, in the weather locations list, there should be a check box to enable/disable rotating to the location without having to delete it.

√ In AdminPanel, for the weather there should be a separate setting for the frequency of updating the forecast data added to the Current Weather update freq (min).

√ In AdminPanel, for the weather, close to the API key, there should be a place to enter the API allocation ( free plan is 60 calls per minute and or 1,000,000 calls per month.

√ Did we address the calculation of allocation of API call with the "Update Frequency" settings? We are allocated 60 calls per minute and/or 1,000,000 apiece call per month.This calculation would stop refrain the user to set the update frequency of the calls in the adminpanel to avoid going over the api call allowance and leave a small buffer.

√ In AdminPanel, the check boxes for the location rotation are now present, but when unchecked, the rotation still rotate them. Actually can we make these toggles? They look better.

√ In AdminPanel, for the weather, close to the API key, Still missing the total per month. The max per minute is 60, but then the max per month is 1 million.

√ the modal "User preferences saved successfully" after saving user settings is not necessary. We can remove it.

√ After saving the Admin setting , is the "Success - Settings saved. Reloading..." Pop-up modal necessary? If not, let's remove it. It is annoying and doesn't bring anything to the UX.

√ In AdminPanel, for the weather locations, we can remove the Master Rotation Toggle. If needing only one location the user can disable all but one.

√ In AdminPanel, for the weather locations, if we there is only one location enabled, we should gray out the toggle to avoid having no locations. When the admin disable all the locations, we get a blank page.

√ UserSettings doesn't seem to have the "profile section anymore where the user can change it's username and email address

√ In UserSettings, we should have a toggle to enable / disable the weather all together. When disabled, the weather widget isn't displayed and the Weather modal is also disabled.

√ In AdminPanel, for the weather locations, instead of disabling the toggle for location when there is only one enabled, if we disable it, this would remove the weather app from the Main page and stop the API calls. The same happens if we delete all the locations. The main page wouldn't have the weather widget and or accessibility to the Weather modal.

√ in UserSettings, we should have the Profile and Security on a separate tab.

√ In AdminPanel, for the weather locations, when toggling the last remaining location, I get the blank page.

√ in UserSettings, clicking on the "LOGS" tab gives me the blank page. Same with Admin > System Settings. Also, the ADMIN > USERS doesn't display the users.

√ We need to move access to the AdminPanel as well as the Log in the UserSettings instead of in separate icons on the main page. A simple user would not have access to the AdminPanel tab and that tab would be hidden. Same with the log for the users without log access.

√ The log export, filter and search are not in the log window anymore.

√ "LOGS" tab in the Admin Panel, when the admin revokes the "Control" permission for a user, the "Take Control" button on that user should become hidden instantly.

√ the Admin. User account should be able to transfer the admin to another user. Like we did with the Group messaging.

√ in a StationCard, the switches should be aligned vertically together and the labels aligned together vertically separately from the switches. We had that previously, but then was removed somehow.

√ Can we have a FlashViewer message send the the user getting the Admin Role when it is transferred?

√ When the cursor overs over the MessageDrawer or a pop-up modal, the background ( main page ) should not scroll while swiping up and down. only when the cursor is off of the MessageDrawer or the pop-up modal, the main page scrolls... when either are showing.

-------------------------------------------------------
-------------------------------------------------------
Requested

- Notes should be different than a simple message.
	- The note window should be like a folder with a list of note titles with their date of last modification and user who created it.
	- Notes are personal to user, but can be shared to selected users. The user the note is shared with has the note with the name of the user who created it.	
	- When clicking on a note, it displays the full content of the note in the FlashViewer ( It should already do that. ) 
	- A user can open one of their note onto another user as a type of memo reminder. This is acting like sending a "FlashMessage" but instead of just being a message that will be lost in the thread, The user can re-use the same note over and over as a recurring reminder or else. If the user receiver of the note FlashViewer already has the note, they still receive the FlashView pop-up modal
	- The note list can be searched for the titles and body.
	- a note can be copied and deleted. If a user copy a shared note from another user, this new copy belongs to the copier, but with a note mentioning it has been copied from user "x"

-----




-------------------------------------------------------
-------------------------------------------------------
Debug




-------------------------------------------------------
-------------------------------------------------------

NOTES



-----------------------------------
MESSAGIN


-----------------------------------

-----------------------------------
WEATHER


-----------------------------------
SERVER

- I need to be able to access the app directly from the Pi Server. Can we set it up so that at boot, the Pi opens the app and can only access the app. Like a kiosk display.



-----------------------------------
UX
- Is it possible for the app to be detect when the screen start to re-arrange the layout when the screen becomes too small to fit as intended. Before re-arranging the layout, reduce the size of the cards and elements to a certain point, then only after reducing the size of the components, then re-organize the layout, but before re-organizing the layout, hide the disabled stations to reduce clutter.

- Let's add 2 user setting for the disabled stations to toggle displaying the offline stations on/off
	- setting 1: if the screen is large enough to show all of the stations without scrolling after resizing, but before rearranging the layouts, show/hide disabled stations
	- setting 2: if the screen is too small to show all of the stations without scrolling after resizing, show/hide disabled stations



- When overing over a component, can we have a tiny comment or pop up explanation of the feature (e.g. overing over "Flash Message" -> pop-up content: Add this option to display the message in a big message viewer on the recipient's screen)

- Can we make the app bi-langual ( French / English )

- When a user have control, the buttons to release should say "Server" and "Cabane", maybe with an icon to indicate to release control. Or a bracket above both with the mention "Release to" or something similar.


-----------------------------------
NATIVE BROWSER ALERTS -> Should be Modal pop-up



-----------------------------------
LAYOUT



-----------------------------------
CONNECTION
- Using DHCP for IP assignment, Once the Server or the Main Controller ( Whoever acts as the DHCP Server ) detects a new station without IP, a valid IP is assigned to it and stored in server setting and in the Arduinos Persistent memory.

ARDUINO
- 
- Main Controller - Slave Mode blinking GREEN -> let's remove the BUZZER and just keep the 2 Thermostat for the Blinking.
- Add Clement alarm. Input from either ST2 or ST3 -> Output to St0


IOS

- Since we can, we should implement app notification when an alert is triggered. Optionally SMS / Email as well as push notification



------------------------------------------------------------
- Cloudflare disable Dev mode: https://dash.cloudflare.com > Caching → Configuration


- Get an complete and updated "setup_db.js" file with all additions to the database.

- Can you give me a guide to fix a "Subnet Mismatch" if I do get that when I move to the another location.

- 
We need an automation system to empty the pipelines before they freeze:
Trigger Drainage of the pipes manually

1. Start all Transport Pumps ( 1sec Push Buttons ): idx 0, 1, 8, 12, 13
	- Trigger Manual or temp below x ˚C
	- Verify all vacuum switches are ON ledPair idx 2, 3, 9, 14 are GREEN -> alarm if any is ON with a RED light.

	Drain T2 ( Transport line 2 ST1 -> Cabane )
	- ST1 idx 5 OFF = Closing Valve T2 (Transport line 2 ST1 -> Cabane)
	- ST1 idx 6 ON = Drain Valve T2 -> ST1

2. Drain the transport line between ST2 and ST3
	- Trigger: When ledPair idx 12 and idx 13 are RED ( Transport Pumps stopped = low level of water in vacuum tank )
	
	Setup Valves to Drain the transport line between ST2 and ST3  
	- ST2 ledPair idx 8 is GREEN -> if not push ON (1 sec ) = Pompe Transport momentary ON ( 1 sec ) to make sure it is working
	- ST3 idx 15 ON = Drain Valve ST2 > ST3
	- ST2 idx 11 ON = Drain Valve ST3 > ST2

2. Drain the transport line between ST1 and ST2 + Close Drain valves for the transport line between ST2 and ST3
	- Trigger: When ST2 ledPair idx 8 is RED ( Transport Pumps stopped = low level of water in vacuum tank )
	
	Setup Valves to Drain the transport line between ST1 and ST2
	- ST1 ledPair idx 0 and 1 are GREEN -> if not push ON (1 sec ) = Transport Pump momentary ON ( 1 sec ) to make sure it is working
	- ST2 idx 10 ON = Drain Valve ST1 > ST2
	- ST1 idx 7 ON = Drain Valve ST2 > ST1

	Close Drain ST3 ( line should be empty by now )
	- ST3 idx 15 OFF = Drain Valve ST2 > ST3

	Close Drain ST1 T2 ( Line should be empty by now )
	- ST1 idx 6 OFF = Drain Valve T2 -> ST1

3. Drain the transport line between Cabane and ST1 + Close Drain valves for the transport line between ST1 and ST2
	- Trigger: When ST1 ledPair idx 0 and 1 are RED ( Transport Pumps stopped = low level of water in vacuum tank )
	
	Setup Valves to Drain the transport line between ST1 and ST2
	- ST1 idx 4 ON = Drain Valve T1 > ST1

	Close Drain ST2 ( line should be empty by now )
	- ST2 idx 10 and 11 OFF = Drain Valve ST2 > ST3

	- Set timer

4. Finish Drainage
	- Trigger: 15 min timer default adjustable in user settings or manual trigger

	Close Drain ST2 ( line should be empty by now )
	- ST1 idx 4 and 7 OFF = Drain Valve T1 > ST1 and ST2 > ST1

	Make sure thermometers are ON
	- Switches idx 22, 24 ON

	



ST3 S3>S3 ON + St2 S3>S2 + ST2 transport + st3 vacuum + ST1 T2 Ferme + T2 Vidange
- When ST2 Tranp stops
- Ferme St3 S3>S3 + St2 S3>S2 + ST2 transport
- ST2 S2>S1 + St1 St2>St1 + ST1 transport
- When ST1 Tranp stops = vide
- Fermer ST2 S2>S1 + St1 St2>St1 + ST1 transport
- St1 Cabane -> vid tuyau 2 + ST Trans 1
- When st1 trans 1 stops
- Vaccumm + vid t1

- Vacc stops at -2˚C



------------------------------------------------------------
------------------------------------------------------------

After debugging:
services/logic_engine.js
function updateStationFeedback() -> comment out " 🛠️ DEBUG HACK: Force ST0 Online if ST1 is Online"

ALARME DU SYSTÈME
PERTE DE VACUUM DÉTECTÉ

RAPPEL
Le système est actif mais le buzzer est muet.
Veuillez activer l’interrupteur du buzzer.




<console>

</console>


<Debug Box>

</Debug Box>


<server.js terminal>

</server.js terminal>







---

Please updated your knowledge. No need to reply. More files are coming.

